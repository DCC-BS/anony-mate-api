services:
  gliner-api:
      image: ghcr.io/dcc-bs/dcc-gliner-api:latest
      ports:
        - "8081:8000"
  docling-serve:
      image: ghcr.io/dcc-bs/dcc-docling-serve-cpu:v1.30.0
      container_name: docling-serve-anony-mate
      ports:
          - ${DOCLING_API_PORT:-5001}:5001
      environment:
          - DOCLING_MODELS_PATH=/models
          - DOCLING_CONFIG_FILE=/app/docling_config.json
          - DOCLING_SERVE_ENABLE_UI=true
      volumes:
          - "${HOME:-/root}/.cache/docling:/models"
  # vllm:
  #   image: vllm/vllm-openai:v0.26.0
  #   container_name: vllm_anony_mate_backend
  #   ports:
  #     - ${LLM_API_PORT}:8000
  #   environment:
  #     - HUGGING_FACE_HUB_TOKEN=${HUGGING_FACE_HUB_TOKEN}
  #   runtime: nvidia
  #   deploy:
  #     resources:
  #       reservations:
  #         devices:
  #           - driver: nvidia
  #             device_ids: ["0"]
  #             capabilities: [gpu]
  #   ipc: host
  #   volumes:
  #     - ${HUGGING_FACE_CACHE_DIR}:/root/.cache/huggingface
  #   command: >
  #     --port 8000
  #     --model Qwen/Qwen3-32B-AWQ
  #     --max-model-len 6000
  #     --gpu-memory-utilization 0.90
  #     --enable-auto-tool-choice
  #     --tool-call-parser hermes
  #     --tensor-parallel-size 1
  #     --max-num-seqs 1
  #     --reasoning-parser deepseek_r1
